#include "main.h"
/**
 * _isupper - check if c is upper
 *  @c: input for alphebet
 *  Return: 1 if its uppercase. 0 if not
*/
int _isupper(int c)
{
if (C >= 65 && C <= 90)
return (1);
else
return (0);
}
